name: Build
on:
  pull_request:
  workflow_dispatch:
  push:

jobs:
  windows_build:
    runs-on: windows-2019   
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v2
        with:
          path: my_repo_path
      - name: Checkout fmt
        uses: actions/checkout@v2
        with:
          repository: fmtlib/fmt
          path: fmt_repo_path
      - name: Setup Visual C++ environment
        uses: ilammy/msvc-dev-cmd@v1
      - name: Setup cmake
        uses: jwlawson/actions-setup-cmake@v1.12
#      - name: Cache proj1 build
#        id: proj1-build
#        uses: actions/cache@v3
#        with:
#          path: my_repo_path\proj1\build
#          key: ${{ runner.os }}-proj1-build
      - name: Build fmt
        run: |
          cmake -S fmt_repo_path -B fmt_repo_path\build -DCMAKE_BUILD_TYPE=Release
          cmake --build fmt_repo_path\build
          cmake --install fmt_repo_path\build
      - name: Build proj1
#        if: steps.proj1-build.outputs.cache-hit != 'true'
        run: |
          cmake -S my_repo_path\proj1 -B my_repo_path\proj1\build -DCMAKE_BUILD_TYPE=Release
          cmake --build my_repo_path\proj1\build
        shell: cmd
      - name: Build proj2
        run: |
          cmake -S my_repo_path\proj2 -B my_repo_path\proj2\build -DCMAKE_BUILD_TYPE=Release
          cmake --build my_repo_path\proj2\build
        shell: cmd
